[package]
name = "icon-gen"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Dev tool: generates the source app icon for `cargo tauri icon`."

[dependencies]
# PNG encoding only — keeps the dependency surface small.
image = { version = "0.25", default-features = false, features = ["png"] }
